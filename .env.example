# Development only. Never put a provider credential in a deployed Vite build.
VITE_GEMINI_API_KEY=

# Production AI requests go to a server-side gateway that stores the provider key.
# Example: https://api.example.edu.tr/ai/generate
VITE_AI_GATEWAY_URL=
