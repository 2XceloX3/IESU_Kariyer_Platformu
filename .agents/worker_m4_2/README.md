# Worker M4.2 Directory
Role: teamwork_preview_worker
Task: Fix 7 test failures in `src/__tests__/AdminDashboard.test.jsx`, `src/__tests__/App.test.jsx`, and test suite so `npx vitest run` passes 100% (156/156 tests passing).
